{
  "name": "sightless-protocol-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^6.0.5"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
